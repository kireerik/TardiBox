{
	"scripts": {
		"start": "micro --listen tcp://localhost:80 upload.js"

		, "dev": "micro-dev --port 80 upload.js"
	}
	, "dependencies": {
		"dotenv": "8.x"

		, "micro": "9.x"

		, "aws-sdk": "2.x"

		, "mime-types": "2.x"
	}
	, "devDependencies": {
		"micro-dev": "3.x"
	}
}